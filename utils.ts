import { Pinecone } from "@pinecone-database/pinecone";

export async function queryPineconeVectorStore(
    client: Pinecone,
    indexName: string,
    namespace: string,
    serachQuery: string
): Promise<string> {
    
    return ""
}

